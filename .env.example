# Produktion: Backend-URL ohne Slash am Ende
# REACT_APP_API_BASE_URL=https://your-backend.onrender.com

# Lokal (npm start): siehe .env.development — empfohlen direktes Flask (CORS ist im Backend aktiv):
# REACT_APP_API_BASE_URL=http://127.0.0.1:5001
# PORT=3002
#
# macOS: Port 3000 kann mit AirTunes/AirPlay kollidieren (POST → HTTP 403). Daher PORT≠3000.
