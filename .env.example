# Produktion: volle Backend-URL (ohne abschliessenden Slash)
REACT_APP_API_BASE_URL=https://your-backend.onrender.com

# Lokal mit `npm start`: Zeile auskommentieren oder leer lassen — dann gehen
# /search und /competency-chain/... über den Proxy in package.json nach :5000
